# Development + offline-analysis dependencies.
-r requirements.txt

# Data drift reporting (heavy; not needed to serve predictions)
evidently==0.7.21

# Testing & linting
pytest==9.1.1
httpx==0.28.1
ruff==0.14.4

# EDA notebook (notebooks/ is EDA-only). matplotlib for plots; nbconvert +
# ipykernel so the notebook can be executed headless (CI does not run it).
matplotlib==3.11.0
nbconvert==7.16.6
ipykernel==7.1.0

# CNN sequence model (rul.deep). CPU build; training only, the API does not use it.
torch==2.14.0

