# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Testing / tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# The large Evidently drift HTML is regenerated by src/rul/drift.py.
# reports/metrics.json (small evaluation record) IS committed.
reports/*.html

# OS
.DS_Store
Thumbs.db

# NOTE: data/ and models/ are intentionally committed so the repo is
# self-contained and the API/Docker image run out of the box. See README.
