# Runtime dependencies (core library + API serving).
# Pinned to the versions verified on Python 3.14. Kept deliberately lean:
# Evidently and test tooling live in requirements-dev.txt so the API image
# stays small.
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.0
lightgbm==4.7.0
mapie==1.5.0
joblib==1.5.2
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
